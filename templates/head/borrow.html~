{% extends 'base.html' %}

{% block content %}

{% if borrowed == 4 %}

<div class="container-fluid">
  <form class="form" method="get">
    <div class="col col-sm-offset-3 col-sm-6">
      <label for="date">Date Lended</label>
      <input class="form-control" name="date" type="text" value="{{ date_val }}" aria-label="">
      <label for="username">Username</label>
      <input class="form-control" name="username" type="text" aria-label="" required autofocus>
      <label for="bookID">Accession Number</label>
      <input class="form-control" name="bookID" type="text" aria-label="" required>
      <hr>
      <input type="submit" class="btn btn-success" value="Lend">
    </div>
  </form>
</div>

{% endif %}


<div class="container-fluid">
    {% if borrowed == 1 %}
        <div class="alert alert-success"><h3>
	<a href="{% url 'profile' username=user.username %}"><div class="label label-info">{{ user.get_name }}</div></a> has borrowed the book <a href="{% url 'book' accNo=book.accession_number %}"><div class="label label-info">{{book.title}}</div>
	  {% elif borrowed == 2 %}
	  <div class="alert alert-danger"><h3>
	      The Book <div class="label label-warning"><a href="{% url 'book' accNo=book.accession_number %}">{{book.title}}</a></div> is currently being borrowed to <div class="label label-warning"><a href="{% url 'profile' username=book.get_borrower.username %}">{{ book.get_borrower.get_name }}</a></div>
	      {% elif borrowed == 3 %}
	      <div class="alert alert-warning">
		The user <a href="{% url 'profile' username=book.get_borrower.username %}">{{ book.get_borrower.username }}</a> has already {{ MAX_NUM_OF_BOOKS_TO_BORROW }} borrowed books
		</div>
	      {% elif borrowed == 0 %}
	  <div class="alert alert-danger"><h3>
	      The Book <div class="label label-warning"><a href="{% url 'book' accNo=book.accession_number %}">{{book.title}}</a></div> is currently being borrowed to <div class="label label-warning"><a href="{% url 'profile' username=book.get_borrower.username %}">{{ book.get_borrower.get_name }}</a></div>
	      {% endif %}
	    </h3>
	  </div> <!-- alert -->
	</div> <!-- container -->
</div>


{% endblock %}
