{% extends 'base.html' %}

{% load static %}
{% block css %}
<link href="{% static 'css/dashboard.css' %}" rel="stylesheet">
{% endblock %}


{% block jumbotron %}
<div class="col col-sm-offset-2 col-sm-10">

  <div class="jumbotron jumbotron-cus">

    <h2><a href="/">Nepal Japan Children's Library</a>  <span style="color:#1CB3C5"> Reading Is Fun</span></h2>

    <p>

      Search our archives for the book you are looking for, then get the book using it's <a href="#call-number-popover"> call number.</a>

    </p>

  </div>
</div>
{% endblock %}



{% block content %}

          <div class="row placeholders">
            <div class="col-xs-6 col-sm-3">
              <div class="circle-head circle-member">
		<span class="text">{{ members_added_during_tp|length }} new & {{ total_members|length }} total</span>
	      </div>
              <h4>Members added in last {{time_period}} months</h4>
              <span class="text-muted">{{ members_added_before_tp|length }} Members added till until {{ start_date }}</span>
            </div>
            <div class="col-xs-6 col-sm-3">
              <div class="circle-book circle-head">
		<span class="text">{{ books_cataloged_during_tp|length}} new & {{total_books|length}} total</span>
	      </div>
              <h4>Books cataloged in last {{time_period}} months</h4>
              <span class="text-muted">{{ books_cataloged_before_tp|length }} books catelogued until {{start_date}}</span>
            </div>
            <div class="col-xs-6 col-sm-3">
              <div class="circle-head circle-fee">
		<span class="text">{Number}</span>
	      </div>
              <h4>Late fees charged in last {{ time_period }} months</h4>
              <span class="text-muted">{} Late fees charged until {{start_date}}</span>
            </div>
            <div class="col-xs-6 col-sm-3">
              <div class="circle-head circle-toy">
		<span class="text">{Number}</span>
	      </div>
              <h4>Toys borrowed in last {{ time_period }} months</h4>
              <span class="text-muted">{} toys borrowed until {{ start_date }}</span>
            </div>
          </div>

          <h2 class="sub-header">Recently Added Books</h2>
          <div class="table-responsive">
            <table class="table table-hover">
              <thead>
                <tr>
		  <th>Accession No.</th>
		  <th>Title</th>
		  <th>Call No.</th>
		  <th>Author</th>
		  <th>Publisher</th>
		  <th>Keywords</th>
		  <th>Date Added</th>
                </tr>
              </thead>
              <tbody>
		{% for book in recently_added_books %}
		<tr>
		  <td>{{book.accession_number}}</td>
		  <td><a href="{% url 'book' book.accession_number %}">{{book.title}}</a></td>
		  <td>{{book.call_number}}</td>
		  <td>{{book.get_authors}}</td>
		  <td>{{book.get_publishers}}</td>
		  <td>{{book.get_keywords}}</td>
		  <td>{{book.accessioned_date}}</td>
		</tr>
		{% endfor %}
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>

    {% endblock %}

    
